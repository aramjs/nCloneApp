export enum ROUTES {
  HOME = "/",
  ADD_LINK = "/add-link",
}

export enum ADMIN_ROUTES {
  HOME = "/admin",
}

export enum COOKIE_KEY {
  USERNAME = "username",
  ADMIN_USERNAME = "admin_username",
}
